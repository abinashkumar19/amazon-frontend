apiVersion: v1
kind: Service
metadata:
  name: amazon-frontend-service
spec:
  selector:
    app: amazon-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: LoadBalancer   # 👈 This creates a cloud Load Balancer (AWS ELB/ALB)
